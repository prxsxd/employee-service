apiVersion: apps/v1
kind: Deployment
metadata:
  name: employee-service

spec:
  replicas: 1

  selector:
    matchLabels:
      app: employee-service

  template:
    metadata:
      labels:
        app: employee-service

    spec:
      containers:
      - name: employee-service
        image: prasad5657/employee-service:1.0
        imagePullPolicy: Always

        ports:
        - containerPort: 1000